import Header from "./components/header";

const Root = () => {
  return (
    <div className="w-full h-full flex flex-col">
      <Header />
      {/* <Outlet />
      <Footer />  */}
    </div>
  );
};

export default Root;
